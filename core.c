#include "vigenere.h"




int main(int argc, char **argv) {
    if(argc != 4) {
        printf(
            "ERROR invalid format: %s <file> <key>",
            argv[0];
        );
        return 0;
    }



    return 0;
}